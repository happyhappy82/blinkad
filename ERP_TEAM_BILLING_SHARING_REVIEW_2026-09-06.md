# ERP 청구·정산 일정 팀 공유 검토

확인일: 2026-09-06 KST. 범위: 운영 화면 읽기, 최신 `erp/ops` 코드 확인, 공유 방식 제안. 기능 수정·배포·초대·알림 발송은 수행하지 않았다.

## 확인한 현재 상태

- 청구관리 직접 링크: https://blinkad-erp-ops.vercel.app/erp?menu=billing
- 정산관리 직접 링크: https://blinkad-erp-ops.vercel.app/erp?menu=settlement
- 두 링크 모두 브라우저에서 해당 화면으로 바로 진입하는 것을 확인했다.
- 운영 왼쪽 메뉴에는 별도 일정관리 메뉴가 없으며, 청구관리 안에 월간 캘린더가 있다.
- 청구관리의 계약·입금·종료·입금요청 일정은 내장 계약 기록에서 계산된다. 수동 추가 일정은 `localStorage`에만 저장된다. 따라서 팀원이 다른 기기에서 추가한 일정이 공유되는 구조가 아니다.
- 정산 API에는 Google Sheets 조회 기능이 있지만, 현재 화면에 넘기는 정산 행은 `contractRevenue.settlementMonths`이다. 시트 연동 상태 문구만 보고 실제 표시값이 시트 원본과 일치한다고 판단하면 안 된다.
- 문서 `docs/ERP_RESUME.md`의 과거 구현 설명과 현재 소스에 차이가 있으므로 위 판단은 현재 소스와 운영 화면에 근거했다.

코드 근거:

- `app/erp/ErpClient.tsx:79` — 메뉴 쿼리 파싱
- `app/erp/ErpClient.tsx:1517` — 화면용 정산 데이터 선택
- `app/erp/ErpClient.tsx:1993` — 현재 청구 화면
- `app/erp/ErpClient.tsx:4364` — 수동 일정 로컬 저장
- `app/erp/ErpClient.tsx:4381` — 내장 계약으로 자동 일정 생성
- `app/erp/ErpClient.tsx:4466` — 수동 일정 저장 처리
- `app/api/erp/settlements/route.ts` — 시트 조회 전용 API

## 추천 구성 (미구현 제안)

1. ERP 공용 원장: 매장별 청구·입금·정산 일정과 처리 상태를 서버에 저장한다. 한 번 입력한 데이터를 팀 화면, 월간 캘린더, 알림이 함께 사용한다.
2. 팀 공유 화면: 오늘·이번 주·입금 지연·담당 매장 필터와 매장별 직접 링크를 제공한다. 팀원별 조회/처리 권한과 수정자·수정 시각을 기록한다.
3. 전용 Google 공유 캘린더: ERP의 확정 일정만 단방향으로 동기화한다. ERP에서 수정한 날짜와 상태를 동일 이벤트에 반영해 중복을 방지한다.
4. 알림: 입금요청 예정일, 입금기한, 기한 경과 미입금을 구분한다. 팀이 선택한 알림 채널에 필요한 항목만 보내고 확인자는 ERP에서 상태를 처리한다. 실제 발송은 별도 요청 후 설정한다.

## 매장별 최소 관리 항목

| 항목 | 의미 |
|---|---|
| 용역비 입금기한 | 고객에게 운영 대행료를 받아야 하는 날 |
| 광고비 입금기한 | 광고 집행용 자금을 받아야 하는 날 |
| 입금요청일 | 담당자가 사전 안내할 날 |
| 외부 정산 지급일 | 작업자·협력사에 송금할 날 |
| 대상 기간 | 어느 운영 회차/기간의 돈인지 |
| 금액·실제 입금액·잔액 | 청구 금액과 실제 수납을 구분 |
| 담당자·상태·마지막 변경 | 누가 처리하고 어디까지 끝냈는지 |

정산 대상월, 서비스 종료일, 실제 고객 입금일, 외부 지급일을 서로 다른 필드로 관리해야 한다. 선납 계약은 이미 낸 기간에 중복 입금요청을 생성하지 않으며, 광고비 잔액과 실제 소진 연동은 별도 단계로 둔다. 입금완료는 확인된 실제 입금 기록에만 표시한다.

## 즉시 가능한 공유

기존 두 직접 링크를 팀 채팅방 공지에 고정하면 페이지 열기는 간단해진다. 다만 수동 일정의 기기 간 공유 문제는 해결되지 않는다. 구현 우선순위는 공용 저장 → 팀 공유 보드 → 캘린더·알림이다.

Google Calendar는 특정 사용자/그룹에게 캘린더를 공유하고 조회·수정 권한을 구분할 수 있다. 공유 캘린더는 아직 생성하거나 초대하지 않았다.

공식 출처: https://support.google.com/calendar/answer/37082?hl=ko

## 남은 확인 사항

- 팀이 평소 사용하는 알림 채널과 팀원별 조회/수정 범위
- 매장별 확정 광고비 입금기한, 협력사 지급일, 실제 입금·선납 내역
- 운영 서버의 시트 연결 결과 및 공유 원장을 어디에 둘지
- 시트/서버와 기존 브라우저 수동 일정을 통합할 때 중복·누락 검증

운영 원장이나 실제 정산 금액의 재검증은 이번 공유 방식 검토에 포함하지 않았다.
